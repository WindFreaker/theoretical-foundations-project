function dropHandler(event) {
    
    event.preventDefault();
    console.log("File(s) dropped");
    
}

function dragOverHandler(event) {
    
    event.preventDefault();
    //add some animation or indication that a file is being uploaded
    
}